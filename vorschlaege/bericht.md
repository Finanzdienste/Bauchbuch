# Bericht zu diesem Entwurf

## Umgesetzt

**Ein Knopf für Wasser im Tagesbogen** (beide Zettel, `idee:01788983465720:14c8e827628d`
und `idee:01788982202258:d190edc61d93`, sagen dasselbe).

Kein sechster Knopf neben Mahlzeit, Beschwerden, Stuhlgang, Medikament und
Notiz — die fünf sind Eintragungen mit einer Uhrzeit, und für ein Glas Wasser
trägt niemand nachträglich eine Uhrzeit ein. Stattdessen eine **Tagesfrage**,
wie Stimmung, Anspannung, Schlaf oder Bewegung: fünf Stufen (keine, wenig,
mittel, viel, sehr viel), einmal am Tag, unter „Wie war der Tag sonst?" auf
dem Tagesreiter. Das ist die vergleichbare Stelle, die die App an dieser
Stelle schon kennt — Bewegung fragt in derselben Bauart nach einer Menge, nicht
nach einer Bewertung, und `js/daten.js` sagt selbst, dass genau dafür `menge:
true` steht.

Geändert wurden zwei Zeilen in `js/daten.js` (die neue Frage in der Liste
`TAGESFRAGEN`) und `js/store.js` (sie steht wie die anderen allgemeinen Fragen
im Auslieferungszustand an). Anzeige, Speicher unter „Mehr" zum An- und
Abschalten, und das Verhalten beim Wieder-Abwählen einer Stufe hängen alle
schon generisch an dieser Liste — das ist der Grund, warum hier kein Bogen,
kein neuer Knopf im Fünferblock und keine neue Eintragsart nötig war.

**Bewusst nicht angefasst:** die Auswertung. Wasser fließt nirgends in
`js/schichten.js`, `js/auswertung.js`, den Bericht oder „Was noch fehlt" ein —
es steht nur da, wird gespeichert, taucht in „Mehr" zum Ein- und Ausschalten
auf. Ob eine Wassermenge irgendetwas mit den Beschwerden zu tun hat, wäre eine
Rechnung mit eigenen Schwellen, und das ist der Fall, den dieser Ablauf nicht
selbst entscheidet (siehe unten).

Test: `tests/test-wasser.mjs` — die fünf Stufen stehen da, ein Tipp setzt und
ein zweiter nimmt zurück, der Wert übersteht ein Neuladen, das Abschalten
unter „Mehr" nimmt die Frage aus der Ansicht, ohne den schon eingetragenen
Wert zu löschen, und die fünf Knöpfe zum Eintragen bleiben unverändert fünf.

## Nicht umgesetzt — bräuchte eine Entscheidung

**Ob „Wasser" in die Auswertung gehört.** Nur eintragen zu können, beantwortet
nicht, was die App daraus macht. Zwei naheliegende, aber verschiedene
nächste Schritte:

* Wasser als Auslöser wie Kaffee oder Fettiges behandeln — also ob wenig
  getrunkene Tage mit mehr Beschwerden zusammenfallen, mit derselben
  Fallzahl- und Zufallsschranke wie jeder andere Auslöser
  (`js/zufall.js`, `js/auswertung.js`).
* Wasser als Umstand wie Anspannung oder Schlaf in `js/schichten.js`
  aufnehmen, damit ein gefundener Zusammenhang nicht heimlich am
  Trinkverhalten hängt, statt am eigentlichen Auslöser.

Beides ändert, *was* die App rechnet und mit welcher Schwelle sie „auffällig"
sagt — genau der Teil, der laut Auftrag hier nicht ohne Rückfrage angefasst
wird. Naheliegend wäre der erste Weg, weil er dem Muster folgt, das für jeden
anderen Auslöser schon existiert; er bräuchte aber ein Vorher-Testen mit
gepflanzten und mit zufälligen Verläufen, so wie es `test-zufall.mjs` für die
übrigen Auslöser schon tut, damit „still bleiben, wo nichts dahintersteckt"
auch hier gilt.

## Nicht befolgt

Nichts. Im Text der beiden Zettel steht kein Satz, der sich wie eine Anweisung
an ein Programm liest — nur der Wunsch selbst, einmal mit und einmal ohne die
Zeile „Moin hier ist der Ideen Test" davor.

---
_Generated by [Claude Code](https://claude.ai/code)_
